# PHP（服务器端脚本语言）

## 语法

<?php

?>

输入：echo/print

换行符：PHP_EOL

## 变量

PHP时弱类型语言

定义  $+字母/_       可带数字

全局/局部变量（作用域不同）

全局：$global    $GLOBAL[]

## 超级全局变量

- $GLOBALS
- $_SERVER
- $_REQUEST   收集HTML表单提交的数据
- $_POST
- $_GET
- $_FILES
- $_ENV
- $_COOKIE
- $_SESSION

## EOF

<<<EOF  （其它字母亦可）

EOF;

## 数据类型及其比较

==：值同

===：值同且类型相同

## 常量

define(标志符，值，（可选）ture对大小写不敏感，默认敏感)

不能加$

## 魔术常量

_LINE_ 

_FILE_

_DIR_

_FUMCTION_

_CLASS_

_TRAIT_

_METHOD_

_NAMESPACE_

### 命名空间

解决问题：1.用户编码与PHP内部冲突

​					2.为较长标识符名称创建短名

写法：namespace 名称{                     

}不加名称即全局变量

命名空间声明前只能有declare语句

层次化命名

类名引用：1.**非限定名称，或不包含前缀的类名称**

​					2.**限定名称,或包含前缀的名称**

​					3.**完全限定名称，或包含了全局前缀操作符的名称**

动态访问

别名/导入

后备全局函数/变量

全局空间

顺序

## 字符串（储存并处理文本）

$string="        ";

并置运算符 ：.               &txt1 . " " . $txt2;

返回字符串长度：strlen()

在字符串中查找一个字符或一段文本：strpos()

## 运算符

注意每个变量都有$        取负：-$x       **取反**：~$x         并置：.

复合/递增/递减运算符：基本与c一致     a.=b即a=a.b

比较：不等于：!=    <>

​			不绝对等于：!==

逻辑

三元运算符："?:"

组合比较符：<=>

## 分支

if...else

sitch(type)    case

## 循环

for        foreach（不太理解）指针渐移

while        do-while

## 数组

$cars=**array("    ","     ")**

数值数组：$cars[0]即数组第一个值

返回数组长度：count()

遍历数组：for循环

遍历关联数组：**foreach（）**

### 数组排序

sort() - 按字母对数组进行升序排列

rsort() - 按字母对数组进行降序排列

asort() - 根据关联数组的值，对数组进行升序排列

ksort() - 根据关联数组的键，对数组进行升序排列

arsort() - 根据关联数组的值，对数组进行降序排列

krsort() - 根据关联数组的键，对数组进行降序排列

## 函数

function  name(添加固定参数)

name（输入参数）

输出：输入参数+固定参数

返回值：return

## 面向对象

对象：类的实例

类：定义事物的抽象特点

​        class	{       }

​		var声明

创建对象：new

​				调用：->

构造函数：创建对象时初始化对象

析构函数：对象结束时，系统执行

继承：extends    子类拥有父类所有东西，但子类不能直接调用父类私有的属性和方法，需要通过父类的public、protected方法调用

方法重写：改写子类

访问控制：public    var定义    默认

​				  protected

​                  private

接口：指定某类必须实现哪些方法（公用）

​		   定义：interface

​           实现：implements

常量：

抽象类：abstract

​              若至少一个方法被声明为抽象则类必须声明为抽象

​			  抽象方法只是声明了调用方式

​              子类必须定义父类中所有抽象方法但可定义新的常数

实例化

关键字：Static      使用self来调用类中的静态成员

​			   Final：类不能被继承，父类方法不能被子类覆盖

调用父类构造：在子类构造方法调用parent::_construct()以执行父类构造方法